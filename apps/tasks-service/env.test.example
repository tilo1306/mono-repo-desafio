# Database Configuration for Tests
# Copy this file to .env.test and adjust the values

DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=postgres
DB_PASSWORD=password
DB_NAME=challenge_db
DB_TEST_NAME=challenge_test_db

# Environment
NODE_ENV=test

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-for-testing-only
