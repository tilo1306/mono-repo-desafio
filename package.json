{
  "name": "mono-repo-desafio",
  "version": "1.0.0",
  "description": "Sistema de Gestão de Tarefas Colaborativo - Desafio Jungle Gaming",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:backend": "turbo run test --filter='./apps/api-gateway' --filter='./apps/auth-service' --filter='./apps/tasks-service' --filter='./apps/notifications-service'",
    "test:frontend": "turbo run test --filter='./apps/web'",
    "test:e2e": "turbo run test:e2e",
    "test:cov": "turbo run test:cov",
    "clean": "turbo run clean",
    "type-check": "turbo run type-check",
    "migration:run": "turbo run migration:run",
    "migration:generate": "turbo run migration:generate",
    "migration:revert": "turbo run migration:revert",
    "migration:test:run": "turbo run db:test:migrate",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "docker:build": "docker-compose build",
    "docker:clean": "docker-compose down -v --remove-orphans"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/tsconfig": "workspace:*",
    "@types/node": "^24.7.2",
    "turbo": "^2.5.8",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "dependencies": {
    "socket.io-client": "^4.8.1"
  }
}
